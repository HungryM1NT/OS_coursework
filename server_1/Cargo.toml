[package]
name = "server_1"
version = "0.1.0"
edition = "2024"

[dependencies]
serde_json = "1.0.140"
common = { path = "../common" }
sysinfo = "0.35.0"
nix = {version="0.30.1", features = ["hostname", "user"]}
